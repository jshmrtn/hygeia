<small>
  <LiveRedirect to={{ Routes.case_protocol_path(@socket, :show, @body.case_uuid) }}>
    {{ case_display_type(@body.case) }}
    <span class="date ml-2">{{ case_display_date(@body.case) }}</span>
    ({{ @body.case.person.first_name }} {{ @body.case.person.last_name }})
  </LiveRedirect>

  {{ pgettext("Notification", "failed to send email.") }}
</small>
